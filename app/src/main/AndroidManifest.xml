<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.InchirieriMasini">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AddEditCarActivity"
            android:exported="false"
            android:label="@string/add_car"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".CarDetailActivity"
            android:exported="false"
            android:label="@string/car_details"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".RentCarActivity"
            android:exported="false"
            android:label="@string/rent_car"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".CarDetailActivity" />

        <activity
            android:name=".RentalsActivity"
            android:exported="false"
            android:label="@string/rentals"
            android:parentActivityName=".MainActivity" />

    </application>

</manifest>
